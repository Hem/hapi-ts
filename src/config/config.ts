import { ServerConfiguration } from "../core/application-params";


export const Config = {
    database: {
        dbType: 'sqlite',
        dbName: 'hapi.sqlite',
        maxConnection: 5
    }
    
};